sudo apt-get install python-pip
sudo apt-get install libxml2-dev libxslt1-dev python-dev

pip install inject
pip install lxml (apt-get install python-lxml)

pip install pycurl (on Linux)
pip install pycurl-7.19.5.1-cp27-none-win_amd64.whl (on Windows http://www.lfd.uci.edu/~gohlke/pythonlibs/#pycurl)

pip install elasticsearch
pip install elasticsearch requests

pip install cssselect 
pip install unidecode